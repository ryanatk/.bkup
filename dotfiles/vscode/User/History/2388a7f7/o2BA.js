import { string } from 'prop-types';

// TODO: update with juan's work

const FloorPlanBuilder = ({ ooId }) => <div>floor plan builder: {ooId}</div>;

FloorPlanBuilder.propTypes = {
  ooId: string,
};

export default FloorPlanBuilder;
