describe('usePartenerUtm with no partner utm in ', () => {
  describe('usePartenerUtm with no partner utm in ', () => {
    it();
  });

  describe('usePartenerUtm with browser', () => {});

  describe('usePartenerUtm with a clean browser', () => {});
});
